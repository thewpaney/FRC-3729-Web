function heading(name, index, array) {
    $('#' + name).hover(

	function() {
	    $('#mainpicture').attr('src', '/assets/' + name + '.jpg');
	    console.log("hover on team");},

	function() {
	    $('#mainpicture').attr('src', '/assets/homepage.jpg');
	    console.log("unhover on team");});
    }

$(document).ready(function() {["team", "blog", "code", "build", "community"].forEach(heading)})
