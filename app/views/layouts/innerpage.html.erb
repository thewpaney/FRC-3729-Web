<%= stylesheet_link_tag "innerpage" %>

<div id="sidebar_div">
  <ul id="sidebar_ul">
    <li><%= link_to "About", "/team/about" %></li>
    <li> <%= link_to "Calendar", "/team/calendar" %></li>

    <% if session[:user] %>
    <li> <%= link_to "Issues", "/issues" %> </li>
    <li></li>
    <% if session[:user].admin? %>
    <li>Mail</li>
    <li>Users</li>
    <% end %>
    <li> <%= link_to "Sign Out", "/logout" %> </li>
    <% else %>
    <li></li>    
    <li> <%= link_to "Sign In", "/login" %></li>
    <% end %>

  </ul>
</div>

<div id="content">
  <%= yield %>
</div>

<%= parent_layout 'application' %>
